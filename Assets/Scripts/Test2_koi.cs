using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test2_koi : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //このscriptは消しても大丈夫です
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
